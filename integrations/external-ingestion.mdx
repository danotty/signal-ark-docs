---
title: "External Ingestion (Partners)"
description: "Guide for external partners to push signals into the SignalArk pipeline."
---

SignalArk accepts external signals from certified data partners (such as Flint) via our dedicated ingestion APIs.

## Authentication

External ingestion uses API Key authentication.
Include your API key in the `Authorization` header:
